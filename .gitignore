# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Regenerable ingest outputs (cohort_labels.json stays tracked)
data/*.parquet
data/quality_report.csv
outputs/__pycache__/
.DS_Store

# Personal roast-session artifacts (regenerable or private-by-default)
reports/
outputs/
/*.alog
.claude/settings.local.json

# Project working documents — not needed to run the software
docs/
ARTISAN_SETTINGS.md
RESEARCH_PROMPT.md
DATA_HUNT_PROMPT.md
MANIFEST.md
manifest.csv
prototypes/

# Externally downloaded training data (unclear/no redistribution license) —
# lives locally only; consented contributions go in data/raw/community/
data/raw/external/

# Personal roast plans/cue cards (generated; test fixtures live in tests/fixtures)
plans/
